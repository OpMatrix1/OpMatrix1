// This is a simple in-memory database for demo purposes only.
// In a real app, use a backend server and persistent storage.

// Remove all logic from this file, as registration and login are now handled by the backend API.

export function registerUser() {
  // No-op
  return { success: false, message: 'Not implemented. Use backend API.' };
}

export function loginUser() {
  // No-op
  return { success: false, message: 'Not implemented. Use backend API.' };
}
